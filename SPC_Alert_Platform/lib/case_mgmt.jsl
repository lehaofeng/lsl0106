Names Default To Here( 1 );

Platform:Namespace( "SPC" );

SPC:OpenCaseWindow := Function( {},
	New Window( "告警&闭环中心", Text Box( "TODO: Case management UI" ) );
);

SPC:AckAlert := Function( {alertId, user},
	Local( {cases = Open( SPC:GetDataDir() || "CASES.jmp", Invisible )} );
	// TODO: Record ack fields and status transition.
	Close( cases, No Save );
);
